{
  "name": "fse-project-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix auth-project\" \"npm start --prefix frontend\"",
    "frontend": "npm start --prefix frontend",
    "auth": "npm run dev --prefix auth-project",
    "build": "npm run build --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
